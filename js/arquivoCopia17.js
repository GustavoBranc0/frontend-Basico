console.log(new Date);
//arrays
//slice

let objetos = [
  'mouse',
  'tesoura',
  'caneta',
  'teclado',
  'notebook',
  'lapis',
  'borracha',
];

console.log(objetos);

// objetos = objetos.slice(0, 3);
// objetos = objetos.slice(1, 3);
objetos = objetos.slice(1);
console.log(objetos);
